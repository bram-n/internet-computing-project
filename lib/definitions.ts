export type Item = {
	itemName: string;
	itemPrice: number;
}

export type User = {
	email: string;
	password: string;
}

export type Movie = {
	id: string;
	Title: string;
	runtimeMinutes: number;
	imdbID: string;
}
