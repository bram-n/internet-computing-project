export type Item = {
	itemName: string;
	itemPrice: number;
}

export type User = {
	email: string;
	password: string;
}

export type Movie = {
	movieID: string;
	movieTitle: string;
	runtimeMinutes: number;
	imdbID: string;
}